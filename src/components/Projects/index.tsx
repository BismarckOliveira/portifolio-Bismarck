import React from 'react';
import {Container, Content} from './styles';

export const Projects: React.FC = () => {
  return (
    <Container>
      <Content>
        <h1>Projetos</h1>
        <span>Todos os projetos Foram feitos do Total zero. Clique na logo para 
          acessar a página dos projetos.
        </span>
      </Content>
    </Container>
  )
}